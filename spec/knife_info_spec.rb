require 'spec_helper'

describe KnifeInfo do
  it 'has a version number' do
    expect(KnifeInfo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
